version: '3.4'

services:
  user-gateway:
    image: user-gateway
    container_name: user-gateway
    restart: on-failure
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 3000:3000
    networks:
      - star-network

networks:
  star-network:
    external:
      name: star-network