version: '3.4'

services:
  user-worker:
    image: user-worker
    container_name: user-worker
    restart: on-failure
    build:
      context: .
      dockerfile: ./Dockerfile
    networks:
      - star-network

networks:
  star-network:
    external:
      name: star-network